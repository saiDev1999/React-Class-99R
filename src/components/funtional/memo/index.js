





// pure component : skip the re-render until its prop or state changes 


// memo : in functional we use React.memo to skip the unneccasary re-renders , components will gets 
// re-render only if it state or prop changes 

// performance improvement technique 

// React.memo is a kind of HOC (Higher order component)

// Hoc : A component passed as a argument to the another component which untimately returns the enhanced component 
// by using this we can re-use the functuality 